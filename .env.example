# Copy this file to .env in the project root (same folder as this file) and adjust if needed.
# The server auto-loads .env — no extra package required.

# Port the combined frontend+API server listens on
PORT=5000

# Secret used to sign login tokens. Change this to a long random string before any real deployment.
JWT_SECRET=sih92-dev-secret-change-me
